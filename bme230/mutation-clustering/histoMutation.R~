library(ggplot2)
library(reshape)


1000genome <- "1000genom_chasm.tab"

ovMut <- read.delim("TCGAOV_mutations.tab",
                              header=T,	
                              row.names=1,
                              stringsAsFactors=F,
                              check.names=F)
